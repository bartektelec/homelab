apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo
  namespace: default

spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /todo
            pathType: Exact
            backend:
              service:
                name: todo
                port:
                  number: 8000
