apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ezbookkeeping-pvc
  namespace: ezbookkeeping
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 1Gi
