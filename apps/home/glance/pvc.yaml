apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: glance-config
  namespace: glance
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
