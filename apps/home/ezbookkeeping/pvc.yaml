apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ezbookkeeping-pvc
  namespace: ezbookkeeping
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
